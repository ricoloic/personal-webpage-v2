import withTitle from '../../hoc/withTitle';
import CircularMotion from './CircularMotion';
import withSketch from '../../hoc/withSketch';

export default withSketch()(withTitle('circularMotion')(CircularMotion));
